# xp_streamdeck — CLine rules

This project already has detailed instructions in `.cursor/rules/*.mdc`.
**Always read and apply those files** (`github-issues-plan-first.mdc`,
`icon-pipeline.mdc`, `project.mdc`, `streamdeck-actions.mdc`,
`xplane-client.mdc`) as if they were part of this ruleset — they are not
optional background info, they are the primary source of truth for how to
work in this repo.

This file exists only as a pointer so the `.cursor/rules` content isn't
missed when working from CLine/CLion instead of Cursor. Do not duplicate
their content here; if the `.cursor/rules` files change, treat the updated
versions as authoritative.
